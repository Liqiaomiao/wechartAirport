<view>
    <picker mode="multiSelector" range="{{range}}" value="{{timecurrent}}" bindchange="bindMultiPickerChange" bindcolumnchange="bindMultiPickerColumnChange"  >
        <view>{{timeText}}</view>
    </picker>
</view>